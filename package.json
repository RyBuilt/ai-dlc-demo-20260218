{
  "name": "task-management-api",
  "version": "1.0.0",
  "description": "REST API for task management",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "concurrently \"node src/server.js\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "test": "jest --verbose",
    "install:all": "npm install && npm --prefix client install"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
